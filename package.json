{
  "name": "guestbot",
  "version": "1.0.0",
  "description": "AI-powered guest concierge for vacation rentals",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
